# NameGnome Serve Environment Configuration
# Copy this file to .env and fill in your values

# Debug mode (set to 1 to enable debug logging)
NAMEGNOME_DEBUG=0

# Cache location (default: ~/.namegnome/cache/namegnome.db)
# NAMEGNOME_CACHE_PATH=/path/to/cache.db

# API Keys (required for provider lookups)
# Get your keys from:
# - TMDB (The Movie Database): https://www.themoviedb.org/settings/api
#   Sign up, request API key, use "API Read Access Token" or "API Key (v3 auth)"
#
# - TVDB (TheTVDB) v3 API: https://thetvdb.com/api-information
#   IMPORTANT: This project uses TVDB v3 API (legacy, requires subscription)
#   v3 uses API key directly, v4 uses different auth - ensure you have v3 access
#
# - MusicBrainz: No API key required, but STRICT rate limits apply
#   See: https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting
#   MUST include User-Agent header, max 1 req/sec (50 req/min implemented below)

TMDB_API_KEY=your_tmdb_api_read_access_token_here
TVDB_API_KEY=your_tvdb_v3_api_key_here
# MusicBrainz requires no key, but needs proper User-Agent

# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=namegnome

# Server configuration
NAMEGNOME_HOST=127.0.0.1
NAMEGNOME_PORT=8000

# Cache TTL (in seconds)
CACHE_TTL_SERIES=2592000
CACHE_TTL_EPISODES=604800
CACHE_TTL_MOVIES=2592000
CACHE_TTL_MUSIC=2592000
CACHE_TTL_DECISIONS=7776000

# Rate limiting (requests per minute)
# These are CONSERVATIVE limits - exceeding these may result in temporary bans!
#
# TMDB: 40 requests per 10 seconds (official limit), we use 40/min to be safe
# TVDB v3: No official public limit documented, 40/min is conservative
# MusicBrainz: STRICT 1 request per second limit, 50/min ensures compliance
#
# DO NOT increase these without consulting official API documentation!
TMDB_RATE_LIMIT=40
TVDB_RATE_LIMIT=40
MUSICBRAINZ_RATE_LIMIT=50

# Logging
LOG_LEVEL=INFO
